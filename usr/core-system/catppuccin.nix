{
    catppuccin = {
        enable = true;
        flavor = "mocha";  # Change this to frappe, macchiato, latte if needed
        rofi = {
            enable = false;
        };
        kitty.enable = true;
        waybar = {
            enable =true;
            mode = "createLink";
        };
        zathura.enable = true;
        btop.enable = true;
      # etc.
    };
}
