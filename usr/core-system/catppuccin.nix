  {
    catppuccin = {
    enable = true;
    flavour = "latte";  # Change this to frappe, macchiato, latte if needed

    integrations = {
      rofi.enable = true;
      kitty.enable = true;
      gtk.enable = true;
      # Add other apps you use:
      zathura.enable = true;
      btop.enable = true;
      alacritty.enable = true;
      # etc.
    };
  };
  }
