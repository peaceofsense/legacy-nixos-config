{
    catppuccin = {
        enable = true;
        flavor = "mocha";  # Change this to frappe, macchiato, latte if needed
        #rofi.enable = true;
        kitty.enable = true;
        # Add other apps you use:
        zathura.enable = true;
        btop.enable = true;
      # etc.
    };
}
